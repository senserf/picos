This is a Manhattan-street version of MNA with two port pairs per station. The
traffic is uniform. All switches are connected to hosts. All links are of the
same length. To create a simulator instance just execute 'mks' in this
directory.

