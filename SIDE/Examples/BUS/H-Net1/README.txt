This is H-Net -- a simple (and unfair) protocol for a dual bus network.
In this version, each station has two packet buffers and two separate
transmitters operating in parallel.
To create a simulator instance, execute 'mks' in this directory.

