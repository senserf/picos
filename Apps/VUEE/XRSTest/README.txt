If you want to run this with UART mode 'L', set UART_TCV_MODE in options.sys
to UART_TCV_MODE_L. For mode 'N', set it to UART_TCV_MODE_N. This also works
for VUEE, but then remember to use single_lu.xml for the 'L' mode and
single.xml in the 'N' mode.
